<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Can thiet de goi Firebase Authentication / Firestore -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Nhan dien giong noi that trong man Luyen phat am (SpeechRecognizer) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Bat buoc tu Android 11 (API 30) de SpeechRecognizer.isRecognitionAvailable() nhan dung
         dich vu nhan dien giong noi cua he thong (package visibility) -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.CalmLearn">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CalmLearn"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.achievement.AchievementListActivity"
            android:exported="false"
            android:theme="@style/Theme.CalmLearn" />

        <activity
            android:name=".ui.achievement.AchievementDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.CalmLearn" />
    </application>

</manifest>
